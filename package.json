{
  "name": "warden",
  "version": "1.0.0",
  "description": "Secure Exam Monitoring Application",
  "author": {
    "name": "Warden Team",
    "email": "warden@example.com"
  },
  "homepage": "https://github.com/warden/warden",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build",
    "test": "npm run test:local",
    "test:local": "npm run test:backend && npm run test:frontend",
    "test:docker": "docker compose exec backend npm run test && docker compose exec frontend npm run test",
    "test:backend": "cd backend && npm run test",
    "test:frontend": "cd frontend && npm run test",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx prisma generate && npm run build",
    "docker:build": "docker build -t dockazawa/warden-backend:latest -f backend/Dockerfile.prod ./backend && docker build -t dockazawa/warden-frontend:latest -f frontend/Dockerfile.prod ./frontend",
    "docker:push": "docker push dockazawa/warden-backend:latest && docker push dockazawa/warden-frontend:latest",
    "docker:publish": "npm run docker:build && npm run docker:push",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}