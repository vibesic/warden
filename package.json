{
  "name": "proctor-app",
  "version": "1.0.0",
  "description": "Secure Exam Proctoring Application",
  "author": {
    "name": "Proctor App Team",
    "email": "proctor@example.com"
  },
  "homepage": "https://github.com/proctor-app/proctor-app",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build",
    "dev:down": "docker compose down",
    "dev:logs": "docker compose logs -f",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm run test",
    "test:frontend": "cd frontend && npm run test",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx prisma generate && npx tsc",
    "build": "npm run build:frontend && npm run build:backend",
    "docker:build": "docker build -t dockazawa/proctor-backend:latest -f backend/Dockerfile.prod ./backend && docker build -t dockazawa/proctor-frontend:latest -f frontend/Dockerfile.prod ./frontend",
    "docker:push": "docker push dockazawa/proctor-backend:latest && docker push dockazawa/proctor-frontend:latest",
    "docker:publish": "npm run docker:build && npm run docker:push",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}